name: Release

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    inputs:
      channel:
        description: 'release to perform'
        required: true
        default: 'nightly'
        type: choice
        options:
        - nightly
  push:
    branches:
    - release

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
        - {os: windows-latest, method: zip}
        - {os: macos-latest, method: gzip}
        - {os: ubuntu-latest, method: gzip}
    steps:
    - uses: actions/checkout@v3
    - uses: dtolnay/rust-toolchain@stable
    - uses: Swatinem/rust-cache@v2
    #- uses: udoprog/kick@nightly
    - run: cargo build --release
    - run: cargo run --release -- compress ${{matrix.method}} --version "${{github.event.inputs.channel}} || nightly" --output dist
    - uses: actions/upload-artifact@v1
      with:
        name: dist-${{matrix.os}}
        path: dist

  publish:
    permissions: write-all
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/download-artifact@v1
      with: {name: dist-windows-latest, path: dist}
    - uses: actions/download-artifact@v1
      with: {name: dist-macos-latest, path: dist}
    - uses: actions/download-artifact@v1
      with: {name: dist-ubuntu-latest, path: dist}
    #- uses: udoprog/kick@nightly
    #- run: kick define --version "${{github.event.inputs.channel}} || nightly" --github-action
    #  id: release
    - uses: udoprog/github-release@v1
      with:
        files: "dist/*"
        #name: ${{steps.release.outputs.version}}
        name: nightly
        token: ${{secrets.GITHUB_TOKEN}}
        #prerelease: ${{steps.release.outputs.pre}}
        prerelease: true
