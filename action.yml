name: 'kick-action'
description: 'Github action that installs and runs a kick command'
author: 'udoprog'

inputs:
  version:
    description: 'Version of kick to install. Defaults to whatever is set for this branch.'
    default: '2026-05-04'

runs:
  using: 'node24'
  main: 'dist/index.js'

branding:
  icon: 'package'
  color: 'orange'
